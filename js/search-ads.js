// featured organizer slider
$('.search_ads').slick({
    centerMode: true,
    centerPadding: '0px',
    slidesToShow: 1,
    slidesToscroll: 1,
    autoplay: false,
    autoplaySpeed: 2000,
    arrows: false,
    dots: true,
    dotsClass:true, 
    centerMode: true,
    focusOnSelect: true
  });
  // createSlick();
  // $(window).on( 'resize', createSlick );
